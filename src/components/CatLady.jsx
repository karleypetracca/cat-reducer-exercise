import React, { useState } from "react";
import { connect } from "react-redux";
import { setName } from "../redux/actions";

const CatLady = () => {
	return (
		<>
			<h1> Cat Lady Cat List</h1>
		</>
	);
};

export default CatLady;
